namespace TaskFlow.Domain.Enums.Planner;

public enum RequirementChangeType
{
    New = 1,
    Changed = 2,
    Removed = 3,
}

public enum RequirementEntityType
{
    Project = 1,
    Task = 2,
    SubTask = 3,
}
